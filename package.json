{
  "name": "foundu",
  "version": "1.0.0",
  "private": true,
  "description": "FoundU — AI-powered campus lost-and-found platform",
  "scripts": {
    "build": "bash build.sh",
    "dev": "bash dev.sh",
    "lint": "bash -c 'export PATH=\"$HOME/Library/pnpm:$PATH\"; pnpm -r lint'",
    "typecheck": "bash -c 'export PATH=\"$HOME/Library/pnpm:$PATH\"; pnpm -r typecheck'",
    "test": "bash -c 'export PATH=\"$HOME/Library/pnpm:$PATH\"; pnpm --filter @foundu/backend test'",
    "db:generate": "bash -c 'export PATH=\"$HOME/Library/pnpm:$PATH\"; pnpm --filter @foundu/db generate'",
    "db:migrate": "bash -c 'export PATH=\"$HOME/Library/pnpm:$PATH\"; pnpm --filter @foundu/db migrate'"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
